Phase Two Project Review

What was the most challanging part?
- Implimenting the logic to handle the image uploads whole maintaining existing data was a big challange. Had to write conditional sql statem to ensure that if a user didnt upload a new photo the old one wasent accidently deleted

What is a success I had during this?
- Integrating the secure authentication system using passowrd_hash() and passoword_verrity().
- This ensures that even if the database is complomised the user passwords remain safe

  What I learned?
  - I kinda learned how to intergrate external APIs, specifacly Google reCAPTCHA v2.
  - This taught me about server to server communication using file_get_contents() and JSON decoding
  - in PHP.
